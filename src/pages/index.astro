---
import { getCollection } from 'astro:content';
import BaseLayout from '../layouts/BaseLayout.astro';

const data4 = await getCollection("song");

const img = data4[0].data.img;
---

<BaseLayout bg={img}>
	<img class="max-h-screen object-contain" src={img} alt="">
	</img>
</BaseLayout>
